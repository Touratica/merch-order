version: "3"
services:
  db:
    image: postgres:alpine
    pull_policy: always
    restart: unless-stopped
    ports:
      - ${POSTGRES_PORT}:5432
    volumes:
      - postgres:/var/lib/postgresql/data
    env_file:
      - .env.local

volumes:
  postgres:
